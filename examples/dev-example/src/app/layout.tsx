import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Yoopta Email Builder — Professional Email Template Editor",
  description:
    "Free and open-source email builder for creating beautiful, responsive email templates. Features real-time preview, customizable components, and HTML export.",
  keywords:
    "rich-text,notion,notion-editor, email builder, email editor, email template builder, react email builder, open source email editor, responsive email templates, html email creator",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
